services:
  magic-uptimekuma:
    container_name: magic-uptimekuma
    image: docker.cnb.cool/magiccode1412/transfer/uptime-kuma:2.4.0
    network_mode: bridge
    restart: unless-stopped
    ports:
      - ${wizard_app_port}:3001
    volumes:
      - ${TRIM_DATA_SHARE_PATHS}/data:/app/data